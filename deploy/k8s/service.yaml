apiVersion: v1
kind: Service
metadata:
  name: oficina-estoque
  namespace: oficina
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: oficina-estoque
  ports:
    - name: http
      port: 80
      targetPort: http
